package com.galactica.gui;

import com.galactica.model.Ship;
import com.galactica.model.ships.DeathStar;
import javafx.scene.image.Image;

/*class ShipImage extends Image {

    ShipImage(Ship ship) {
        String path = "";
        if (ship instanceof DeathStar) {
            path = getClass().getResource("/assets/deathstar.png").toExternalForm();
        }

        super(path);

   
} */
