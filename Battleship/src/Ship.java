public class Ship{
    private int length;
    private boolean sunk;  //Status of the ship, true if ship is sunk, false if not

    public Ship(int length){
        this.length = length;
        this.sunk = false;
    }
    
}